import styled from 'styled-components'
import { darkGray } from 'styles/colors'
import { big, bold } from 'styles/fonts'

export const Title = styled.label`
  font-size: ${big};
  color: ${darkGray};
  font-weight: ${bold};
  margin-bottom: 20px;
`
